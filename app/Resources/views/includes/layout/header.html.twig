<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('homepage') }}">Deep Space Objects</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownCat" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Catalogues
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownCat">
          <a class="dropdown-item disabled" href="#">All catalogues</a>
          <div class="dropdown-divider"></div>

          {% set listCatalog = ['messier', 'ngc', 'ic', 'ldn', 'sh', 'cr', 'sto', 'rcw', 'abl'] %}
          {% for catalog in listCatalog %}
            <a
              class="dropdown-item"
              href="{{ path('catalog_list', {'catalog': catalog}) }}"
            >
              {% set label = 'catalog.' ~ catalog %}
              {{ label|trans }}
            </a>
          {% endfor %}
        </div>
      </li>


      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownConst" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          {{ 'const'|transchoice(2) }}
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownConst">
          <a class="dropdown-item" href="">{{ 'hem.no'|trans }}</a>
          <a class="dropdown-item" href="">{{ 'hem.su'|trans }}</a>
        </div>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Planets</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Map</a>
      </li>
    </ul>
  </div>

  {% include ':includes/layout:switchLang.html.twig' %}
</nav>
