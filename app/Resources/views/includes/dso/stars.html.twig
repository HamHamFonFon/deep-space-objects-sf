{% spaceless %}
  {% if stars is defined and 0 < stars %}
    {% set starHtml = '' %}
    {% for star in range(1, stars) %}
      {% set starHtml = starHtml ~ '<span class="icon-star"></span>' %}
    {% endfor %}
    {{ starHtml|trim|raw }}
  {% endif %}
{% endspaceless %}